server {
  listen 80;

  location / {
    root /usr/src/app/dist;
    index index.html;
    try_files $uri $uri/ /index.html;
  }

  location /api/ {
    rewrite ^/api/(.*)$ /$1 break;
    proxy_pass http://app:8888;
  }
}